pipeline {
    agent any

    tools {
        maven "maven-3.9"
    }

    environment {
        SETTINGS = credentials('maven-settings')
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build & Test') {
            steps {
                sh "mvn -s $SETTINGS -f app/pom.xml clean install"
            }
        }

        stage('Publish to Nexus') {
            when {
                branch 'main'
            }
            steps {
                sh "mvn -s $SETTINGS -f app/pom.xml deploy"
            }
        }

        stage('Deploy to Test') {
            when {
                branch 'main'
            }
            steps {
                sh """
		    echo "[CI/CD] Deploy to TEST"
                    docker cp ${env.WORKSPACE}/app/target/myapp-1.0.0.war test1:/opt/tomcat/webapps/app.war
                    docker restart test1
                """
            }
        }

        stage('Approve Prod Deployment') {
            when {
                branch 'main'
            }
            steps {
                input message: "Czy wdrożyć na PROD?"
            }
        }

        stage('Deploy to Prod') {
            when {
                branch 'main'
            }
            steps {
                sh """
		    echo "[CI/CD] Deploy to TEST"
                    docker cp ${env.WORKSPACE}/app/target/myapp-1.0.0.war test1:/opt/tomcat/webapps/app.war
                    docker restart test1
                """
            }
        }
    }
}
